<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% block title %}TGV Delivery - Plateforme partenaires{% endblock %}</title>

    {# Bootstrap 5 #}
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">

    {# Google Fonts #}
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">

    <style>
        body {
            font-family: "Inter", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            background-color: #0f172a;
            color: #e5e7eb;
        }

        .navbar {
            backdrop-filter: blur(12px);
            background: rgba(15, 23, 42, 0.85);
            border-bottom: 1px solid rgba(148, 163, 184, 0.25);
        }

        .hero {
            min-height: 60vh;
            display: flex;
            align-items: center;
            justify-content: center;
            text-align: center;
            padding: 4rem 1rem 3rem;
            background: radial-gradient(circle at top left, #1d4ed8, transparent 55%),
                        radial-gradient(circle at bottom right, #f97316, transparent 55%);
        }

        .hero-card {
            background: rgba(15, 23, 42, 0.92);
            border-radius: 1.5rem;
            padding: 3rem 2rem;
            box-shadow:
                0 24px 60px rgba(15, 23, 42, 0.9),
                0 0 0 1px rgba(148, 163, 184, 0.2);
        }

        .hero-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.3rem 0.8rem;
            border-radius: 999px;
            background: rgba(15, 23, 42, 0.9);
            border: 1px solid rgba(148, 163, 184, 0.35);
            color: #e5e7eb;
            font-size: 0.8rem;
            text-transform: uppercase;
            letter-spacing: 0.08em;
        }

        .hero-badge-dot {
            width: 8px;
            height: 8px;
            border-radius: 999px;
            background: #22c55e;
            box-shadow: 0 0 0 6px rgba(34, 197, 94, 0.25);
        }

        .hero-title {
            font-size: clamp(2.3rem, 3vw, 3.1rem);
            font-weight: 700;
            letter-spacing: -0.04em;
            color: #f9fafb;
            margin-bottom: 1rem;
        }

        .hero-subtitle {
            font-size: 1rem;
            color: #9ca3af;
            max-width: 34rem;
            margin: 0 auto 2rem;
        }

        .hero-actions .btn-primary {
            background: linear-gradient(135deg, #2563eb, #4f46e5);
            border: none;
            padding-inline: 1.8rem;
            box-shadow: 0 18px 40px rgba(37, 99, 235, 0.55);
        }

        .hero-actions .btn-outline-light {
            border-color: rgba(148, 163, 184, 0.4);
            color: #e5e7eb;
        }

        .hero-metrics {
            margin-top: 2.3rem;
            display: flex;
            justify-content: center;
            gap: 2.5rem;
            flex-wrap: wrap;
            color: #9ca3af;
            font-size: 0.9rem;
        }

        .hero-metrics strong {
            display: block;
            color: #e5e7eb;
            font-size: 1.2rem;
        }

        .section-title {
            font-size: 1.4rem;
            font-weight: 600;
            color: #f9fafb;
        }

        .card-glass {
            background: rgba(15, 23, 42, 0.9);
            border-radius: 1rem;
            border: 1px solid rgba(148, 163, 184, 0.3);
            box-shadow:
                0 18px 40px rgba(15, 23, 42, 0.8),
                0 0 0 1px rgba(148, 163, 184, 0.25);
        }

        footer {
            border-top: 1px solid rgba(148, 163, 184, 0.3);
            background: #020617;
            color: #6b7280;
        }
    </style>

    {% block stylesheets %}{% endblock %}
</head>
<body>
<nav class="navbar navbar-expand-lg navbar-dark sticky-top">
    <div class="container">
        <a class="navbar-brand d-flex align-items-center fw-semibold" href="{{ path('app_home') }}">
            <span class="fw-bold text-white">TGV</span><span class="text-primary ms-1">Delivery</span>
        </a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
            <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav ms-auto align-items-lg-center">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_partenaire_index') }}">Partenaires</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('app_zone_index') }}">Zones</a>
                </li>
            </ul>
        </div>
    </div>
</nav>

<main>
    {% block body %}{% endblock %}
</main>

<footer class="py-4 mt-5">
    <div class="container d-flex flex-column flex-md-row justify-content-between align-items-center gap-2">
        <span>© {{ "now"|date("Y") }} TGV Delivery. Tous droits réservés.</span>
        <span class="small text-secondary">
            Projet PIDEV - Gestion des partenaires & zones de livraison
        </span>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
{% block javascripts %}{% endblock %}
</body>
</html>

