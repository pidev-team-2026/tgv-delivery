<div class="form-container">
    <h2 class="form-title">{{ button_label|default('Gérer Produit') }}</h2>
    
    {{ form_start(form) }}
    
    <div class="form-row">

        <div class="form-group">
            {{ form_row(form.nom) }}
        </div>
    </div>

    <div class="form-group form-group-full">
        {{ form_row(form.description) }}
    </div>

    <div class="form-row">
        <div class="form-group">
            {{ form_row(form.prix) }}
        </div>
        <div class="form-group">
            {{ form_row(form.stock) }}
        </div>
    </div>

    <div class="form-row">
        <div class="form-group">
            {{ form_row(form.image) }}
        </div>
        <div class="form-group">
            {{ form_row(form.categorie) }}
        </div>
    </div>

    <div class="form-row">
        <div class="form-group">
            {{ form_row(form.statut) }}
        </div>
        <div class="form-group">
            {{ form_row(form.promotion) }}
        </div>
    </div>

    <div style="display: flex; gap: 1rem; margin-top: 2rem;">
        <button type="submit" class="btn-form btn-form-primary">{{ button_label|default('Enregistrer') }}</button>
        <a href="{{ path('wajih_produit_index') }}" class="btn-form btn-form-secondary" style="text-decoration: none; display: inline-flex; align-items: center;">Annuler</a>
    </div>
    {{ form_end(form) }}
</div>
