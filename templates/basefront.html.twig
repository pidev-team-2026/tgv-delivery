<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="utf-8">
    <meta content="width=device-width, initial-scale=1.0" name="viewport">
    <title>{% block title %}TGV Delivery{% endblock %}</title>
    <meta name="description" content="TGV Delivery - Gestion des réclamations clients">

    <!-- Google Fonts - Inter -->
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">

    <!-- Bootstrap 5 -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet"
          integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">

    <style>
        :root {
            --tgv-bg: #020617;
            --tgv-blue-navy: #0b1220;
            --tgv-blue-soft: #1d2a3f;
            --tgv-border-subtle: rgba(148, 163, 184, 0.4);
            --tgv-text-main: #e5e7eb;
            --tgv-text-muted: #9ca3af;
            --tgv-accent-blue: #2563eb;
            --tgv-accent-violet: #7c3aed;
            --tgv-accent-orange: #f97316;
            --tgv-card-radius: 24px;
        }

        html, body {
            height: 100%;
        }

        body.tgv-body {
            margin: 0;
            min-height: 100vh;
            background-color: var(--tgv-bg);
            color: var(--tgv-text-main);
            font-family: "Inter", system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
            -webkit-font-smoothing: antialiased;
        }

        .tgv-main {
            padding-top: 80px; /* navbar height offset */
        }

        .tgv-navbar {
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 1030;
            background: rgba(15, 23, 42, 0.85); /* bleu nuit transparent */
            backdrop-filter: blur(18px);
            -webkit-backdrop-filter: blur(18px);
            border-bottom: 1px solid var(--tgv-border-subtle);
        }

        .tgv-navbar .navbar-brand img {
            height: 32px;
            width: auto;
        }

        .tgv-navbar .nav-link {
            color: var(--tgv-text-muted);
            font-weight: 500;
            font-size: 0.95rem;
            letter-spacing: 0.02em;
        }

        .tgv-navbar .nav-link:hover,
        .tgv-navbar .nav-link.active {
            color: #f9fafb;
        }

        .hero-section {
            min-height: calc(100vh - 80px);
            display: flex;
            align-items: center;
            padding: 5rem 1.5rem 4rem;
            background:
                radial-gradient(circle at top left, rgba(56, 189, 248, 0.35), transparent 55%),
                radial-gradient(circle at top right, rgba(129, 140, 248, 0.35), transparent 55%),
                radial-gradient(circle at bottom center, rgba(249, 115, 22, 0.4), transparent 60%),
                radial-gradient(circle at center, rgba(15, 23, 42, 0.95), #020617);
        }

        .tgv-hero-card {
            background: radial-gradient(circle at top left, rgba(37, 99, 235, 0.25), transparent 40%),
                        radial-gradient(circle at bottom right, rgba(88, 28, 135, 0.3), transparent 45%),
                        rgba(15, 23, 42, 0.96);
            border-radius: var(--tgv-card-radius);
            border: 1px solid rgba(148, 163, 184, 0.35);
            box-shadow:
                0 24px 60px rgba(15, 23, 42, 0.9),
                0 0 0 1px rgba(15, 23, 42, 0.8);
            padding: 2.5rem 2.25rem;
        }

        @media (min-width: 992px) {
            .tgv-hero-card {
                padding: 3rem 3rem;
            }
        }

        .tgv-badge {
            display: inline-flex;
            align-items: center;
            gap: 0.5rem;
            padding: 0.4rem 0.9rem;
            border-radius: 999px;
            border: 1px solid rgba(52, 211, 153, 0.35);
            background: radial-gradient(circle at left, rgba(34, 197, 94, 0.45), transparent 60%),
                        rgba(15, 23, 42, 0.85);
            color: #bbf7d0;
            font-size: 0.75rem;
            font-weight: 600;
            letter-spacing: 0.09em;
            text-transform: uppercase;
        }

        .tgv-badge-dot {
            width: 8px;
            height: 8px;
            border-radius: 999px;
            background: #22c55e;
            box-shadow: 0 0 0 4px rgba(34, 197, 94, 0.35);
        }

        .tgv-hero-title {
            margin-top: 1.5rem;
            font-weight: 800;
            font-size: clamp(2.15rem, 3.1vw + 1.4rem, 2.9rem);
            line-height: 1.1;
            letter-spacing: -0.04em;
            color: #f9fafb;
        }

        .tgv-hero-description {
            margin-top: 1rem;
            font-size: 0.98rem;
            line-height: 1.7;
            color: var(--tgv-text-muted);
            max-width: 44rem;
        }

        .tgv-hero-actions {
            margin-top: 1.75rem;
            display: flex;
            flex-wrap: wrap;
            gap: 0.9rem;
            justify-content: center;
        }

        @media (min-width: 768px) {
            .tgv-hero-actions {
                justify-content: flex-start;
            }
        }

        .btn-gradient-primary {
            position: relative;
            overflow: hidden;
            border-radius: 999px;
            padding: 0.7rem 1.6rem;
            font-weight: 600;
            font-size: 0.95rem;
            border: none;
            color: #f9fafb;
            background-image: linear-gradient(to right, var(--tgv-accent-blue), var(--tgv-accent-violet));
            box-shadow:
                0 18px 45px rgba(37, 99, 235, 0.5),
                0 0 0 1px rgba(37, 99, 235, 0.6);
        }

        .btn-gradient-primary:hover {
            color: #f9fafb;
            background-image: linear-gradient(to right, #1d4ed8, #6d28d9);
        }

        .btn-outline-light-rounded {
            border-radius: 999px;
            padding: 0.7rem 1.6rem;
            font-weight: 500;
            font-size: 0.95rem;
            color: #e5e7eb;
            border: 1px solid rgba(209, 213, 219, 0.6);
            background: transparent;
        }

        .btn-outline-light-rounded:hover {
            background: rgba(15, 23, 42, 0.9);
            color: #f9fafb;
        }

        .tgv-metrics {
            margin-top: 2.25rem;
        }

        .tgv-metric-title {
            font-size: 0.95rem;
            font-weight: 600;
            color: #e5e7eb;
        }

        .tgv-metric-text {
            margin-top: 0.3rem;
            font-size: 0.9rem;
            color: var(--tgv-text-muted);
        }

        .tgv-section {
            background: radial-gradient(circle at top, rgba(15, 23, 42, 0.95), #020617);
        }

        .card-glass {
            background: radial-gradient(circle at top left, rgba(30, 64, 175, 0.3), transparent 55%),
                        rgba(15, 23, 42, 0.9);
            border-radius: 1.4rem;
            border: 1px solid rgba(148, 163, 184, 0.3);
            box-shadow:
                0 18px 35px rgba(15, 23, 42, 0.9),
                0 0 0 1px rgba(15, 23, 42, 0.9);
            padding: 1.75rem 1.5rem;
        }

        .card-glass h3 {
            font-size: 1.05rem;
            font-weight: 600;
            color: #e5e7eb;
            margin-bottom: 0.6rem;
        }

        .card-glass p {
            font-size: 0.92rem;
            color: var(--tgv-text-muted);
            margin: 0;
        }

        .tgv-footer {
            border-top: 1px solid rgba(31, 41, 55, 0.9);
            background-color: #020617;
            padding: 1.5rem 0;
            font-size: 0.85rem;
            color: var(--tgv-text-muted);
        }
    </style>

    {% block stylesheets %}{% endblock %}
</head>
<body class="tgv-body">

<header class="tgv-navbar">
    <nav class="navbar navbar-expand-lg navbar-dark">
        <div class="container py-2">
            <a class="navbar-brand d-flex align-items-center" href="{{ path('app_front_home') }}">
                <img src="{{ asset('front/assets/img/LOGO.png') }}" alt="TGV Delivery">
            </a>
            <button class="navbar-toggler border-0" type="button" data-bs-toggle="collapse"
                    data-bs-target="#tgvNavbar" aria-controls="tgvNavbar" aria-expanded="false"
                    aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse justify-content-end" id="tgvNavbar">
                <ul class="navbar-nav align-items-lg-center gap-lg-2">
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_front_home' %}active{% endif %}"
                           href="{{ path('app_front_home') }}">
                            Accueil
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') == 'app_front_select_user' %}active{% endif %}"
                           href="{{ path('app_front_select_user') }}">
                            Connexion
                        </a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link {% if app.request.attributes.get('_route') starts with 'app_front_reclamation' %}active{% endif %}"
                           href="{{ path('app_front_reclamation_index') }}">
                            Mes réclamations
                        </a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
</header>

<main class="tgv-main">
    {% block body %}{% endblock %}
</main>

<footer class="tgv-footer">
    <div class="container">
        <div class="row align-items-center gy-2">
            <div class="col-md-6 text-center text-md-start">
                <span>© {{ "now"|date("Y") }} TGV Delivery. Tous droits réservés.</span>
            </div>
            <div class="col-md-6 text-center text-md-end">
                <span>Projet PIDEV - Gestion des réclamations clients</span>
            </div>
        </div>
    </div>
</footer>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"
        integrity="sha384-YvpcrYf0tY3lHB60NNkmXc5s9fDVZLESaAA55NDzOxhy9GkcIdslK1eN7N6jIeHz"
        crossorigin="anonymous"></script>

{% block javascripts %}{% endblock %}

</body>
</html>
