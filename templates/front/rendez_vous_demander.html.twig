{% extends 'front/base.html.twig' %}

{% block title %}Demander un rendez-vous — Gestion des services{% endblock %}

{% block body %}
<h1 class="h4 fw-semibold mb-3">Demander un rendez-vous</h1>
<p class="text-muted small mb-4">Choisissez un commerçant et indiquez la date et le message. Le commerçant acceptera ou refusera votre demande.</p>

<div class="row justify-content-center">
    <div class="col-lg-6">
        <div class="card admin-card">
            <div class="card-body">
                {{ form_start(form, { attr: { novalidate: 'novalidate' }, row_attr: { class: 'form-group-custom' } }) }}
                    {{ form_row(form.commercant, { row_attr: { class: 'form-group-custom' }, label_attr: { class: 'form-label' }, attr: { class: 'form-select' } }) }}
                    {{ form_row(form.emailDemandeur, { row_attr: { class: 'form-group-custom' }, label_attr: { class: 'form-label' }, attr: { class: 'form-control' } }) }}
                    {{ form_row(form.dateRdv, { row_attr: { class: 'form-group-custom' }, label_attr: { class: 'form-label' }, attr: { class: 'form-control' } }) }}
                    {{ form_row(form.message, { row_attr: { class: 'form-group-custom' }, label_attr: { class: 'form-label' }, attr: { class: 'form-control' } }) }}
                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">Envoyer la demande</button>
                    </div>
                {{ form_end(form) }}
            </div>
        </div>
        <p class="mt-3 text-center"><a href="{{ path('app_front_commercants_list') }}" class="btn btn-outline-secondary btn-sm">Retour à la liste des commerçants</a></p>
    </div>
</div>
{% endblock %}
