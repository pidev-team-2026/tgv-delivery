{% extends 'front/base.html.twig' %}

{% block title %}TGV Delivery - Plateforme de livraison professionnelle{% endblock %}

{% block navbar %}{% endblock %}

{% block stylesheets %}
<link rel="preconnect" href="https://fonts.googleapis.com">
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
<style>
* {
    margin: 0;
    padding: 0;
    box-sizing: border-box;
}

body {
    font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', sans-serif;
    background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
    color: #ffffff;
    overflow-x: hidden;
}

/* Navbar */
.navbar-delivery {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    background: rgba(15, 23, 42, 0.8);
    backdrop-filter: blur(12px);
    -webkit-backdrop-filter: blur(12px);
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
    padding: 1rem 0;
}

.navbar-container {
    max-width: 1200px;
    margin: 0 auto;
    padding: 0 2rem;
    display: flex;
    justify-content: space-between;
    align-items: center;
}

.navbar-brand {
    display: flex;
    align-items: center;
    text-decoration: none;
}

.navbar-logo {
    height: 32px;
    width: auto;
}

.navbar-links {
    display: flex;
    gap: 2rem;
    list-style: none;
}

.navbar-links a {
    color: #e2e8f0;
    text-decoration: none;
    font-weight: 500;
    font-size: 0.95rem;
    transition: color 0.3s ease;
}

.navbar-links a:hover {
    color: #ffffff;
}

/* Hero Section */
.hero-section {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 6rem 2rem 4rem;
    background: radial-gradient(ellipse at top, #3b82f6 0%, #8b5cf6 25%, #f97316 50%, #0f172a 70%);
    position: relative;
}

.hero-section::before {
    content: '';
    position: absolute;
    top: 0;
    left: 0;
    right: 0;
    bottom: 0;
    background: radial-gradient(circle at 20% 50%, rgba(59, 130, 246, 0.3) 0%, transparent 50%),
                radial-gradient(circle at 80% 80%, rgba(139, 92, 246, 0.3) 0%, transparent 50%),
                radial-gradient(circle at 40% 20%, rgba(249, 115, 22, 0.2) 0%, transparent 50%);
    pointer-events: none;
}

.hero-card {
    background: rgba(15, 23, 42, 0.9);
    backdrop-filter: blur(20px);
    -webkit-backdrop-filter: blur(20px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 24px;
    padding: 3rem;
    max-width: 800px;
    width: 100%;
    box-shadow: 0 25px 50px rgba(0, 0, 0, 0.3);
    text-align: center;
    position: relative;
    z-index: 1;
}

.hero-badge {
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
    background: rgba(16, 185, 129, 0.1);
    border: 1px solid rgba(16, 185, 129, 0.3);
    border-radius: 50px;
    padding: 0.5rem 1rem;
    margin-bottom: 2rem;
    font-size: 0.875rem;
    color: #10b981;
    font-weight: 500;
}

.badge-dot {
    width: 8px;
    height: 8px;
    background: #10b981;
    border-radius: 50%;
    box-shadow: 0 0 10px rgba(16, 185, 129, 0.8);
    animation: pulse 2s infinite;
}

@keyframes pulse {
    0%, 100% { opacity: 1; }
    50% { opacity: 0.5; }
}

.hero-title {
    font-size: 3.5rem;
    font-weight: 800;
    line-height: 1.1;
    margin-bottom: 1.5rem;
    background: linear-gradient(135deg, #ffffff 0%, #e2e8f0 100%);
    -webkit-background-clip: text;
    -webkit-text-fill-color: transparent;
    background-clip: text;
}

.hero-subtitle {
    font-size: 1.25rem;
    color: #94a3b8;
    line-height: 1.6;
    margin-bottom: 3rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
}

.hero-buttons {
    display: flex;
    gap: 1rem;
    justify-content: center;
    margin-bottom: 3rem;
    flex-wrap: wrap;
}

.btn-hero {
    padding: 1rem 2rem;
    border-radius: 12px;
    font-weight: 600;
    font-size: 1rem;
    text-decoration: none;
    transition: all 0.3s ease;
    display: inline-flex;
    align-items: center;
    gap: 0.5rem;
}

.btn-primary {
    background: linear-gradient(135deg, #3b82f6, #8b5cf6);
    color: #ffffff;
    border: none;
    box-shadow: 0 4px 15px rgba(59, 130, 246, 0.4);
}

.btn-primary:hover {
    transform: translateY(-2px);
    box-shadow: 0 8px 25px rgba(59, 130, 246, 0.6);
}

.btn-outline {
    background: transparent;
    color: #ffffff;
    border: 2px solid rgba(255, 255, 255, 0.3);
}

.btn-outline:hover {
    background: rgba(255, 255, 255, 0.1);
    border-color: rgba(255, 255, 255, 0.5);
    transform: translateY(-2px);
}

/* Metrics */
.hero-metrics {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(200px, 1fr));
    gap: 2rem;
    text-align: left;
}

.metric-item h3 {
    font-size: 1.25rem;
    font-weight: 700;
    color: #ffffff;
    margin-bottom: 0.5rem;
}

.metric-item p {
    color: #94a3b8;
    font-size: 0.95rem;
    line-height: 1.5;
}

/* Features Section */
.features-section {
    padding: 6rem 2rem;
    background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%);
}

.features-container {
    max-width: 1200px;
    margin: 0 auto;
}

.features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
    gap: 2rem;
}

.card-glass {
    background: rgba(15, 23, 42, 0.6);
    backdrop-filter: blur(10px);
    -webkit-backdrop-filter: blur(10px);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 16px;
    padding: 2.5rem;
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.2);
    transition: transform 0.3s ease, box-shadow 0.3s ease;
}

.card-glass:hover {
    transform: translateY(-5px);
    box-shadow: 0 20px 40px rgba(0, 0, 0, 0.3);
}

.card-glass h3 {
    font-size: 1.5rem;
    font-weight: 700;
    color: #ffffff;
    margin-bottom: 1rem;
}

.card-glass p {
    color: #94a3b8;
    line-height: 1.6;
    font-size: 0.95rem;
}

/* Footer */
.footer {
    background: #020617;
    border-top: 1px solid rgba(255, 255, 255, 0.1);
    padding: 3rem 2rem;
    text-align: center;
}

.footer-container {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 2rem;
    align-items: center;
}

.footer p {
    color: #64748b;
    font-size: 0.9rem;
}

/* Responsive */
@media (max-width: 768px) {
    .navbar-container {
        padding: 0 1rem;
    }
    
    .navbar-links {
        gap: 1rem;
    }
    
    .hero-section {
        padding: 5rem 1rem 3rem;
    }
    
    .hero-card {
        padding: 2rem;
    }
    
    .hero-title {
        font-size: 2.5rem;
    }
    
    .hero-subtitle {
        font-size: 1.1rem;
    }
    
    .hero-buttons {
        flex-direction: column;
        align-items: center;
    }
    
    .btn-hero {
        width: 100%;
        max-width: 300px;
        justify-content: center;
    }
    
    .hero-metrics {
        grid-template-columns: 1fr;
        text-align: center;
    }
    
    .features-grid {
        grid-template-columns: 1fr;
    }
    
    .footer-container {
        grid-template-columns: 1fr;
        text-align: center;
        gap: 1rem;
    }
}
</style>
{% endblock %}

{% block body %}
<!-- Navbar -->
<nav class="navbar-delivery">
    <div class="navbar-container">
        <a href="{{ path('app_front_accueil') }}" class="navbar-brand">
            <img src="{{ asset('admin/images/logo-tgv-delivery.png') }}" alt="TGV Delivery" class="navbar-logo">
        </a>
        <ul class="navbar-links">
            <li><a href="{{ path('app_front_accueil') }}">Accueil</a></li>
            <li><a href="{{ path('app_front_commercants_list') }}">Commerçants</a></li>
            <li><a href="{{ path('app_front_rendez_vous_demander') }}">Demander un RDV</a></li>
            <li><a href="{{ path('app_commercant_index') }}">Espace admin</a></li>
        </ul>
    </div>
</nav>

<!-- Hero Section -->
<section class="hero-section">
    <div class="hero-card">
        <div class="hero-badge">
            <span class="badge-dot"></span>
            PROJET PIDEV & SYMFONY
        </div>
        
        <h1 class="hero-title">
            Pilotez vos partenaires et zones de livraison comme un professionnel.
        </h1>
        
        <p class="hero-subtitle">
            TGV Delivery connecte vos commerçants, livreurs et clients autour d'un suivi simple, fiable et en temps réel. 
            Gérez vos partenaires, organisez vos zones de livraison et garantissez des colis livrés à l'heure, partout en Tunisie.
        </p>
        
        <div class="hero-buttons">
            <a href="{{ path('app_front_commercants_list') }}" class="btn-hero btn-primary">
                <i class="fas fa-users"></i>
                Voir les commerçants
            </a>
            <a href="{{ path('app_front_rendez_vous_demander') }}" class="btn-hero btn-outline">
                <i class="fas fa-calendar-check"></i>
                Prendre rendez-vous
            </a>
        </div>
        
        <div class="hero-metrics">
            <div class="metric-item">
                <h3>Experts de la livraison</h3>
                <p>Une équipe de professionnels dédiés à l'excellence de chaque livraison.</p>
            </div>
            <div class="metric-item">
                <h3>Couverture nationale</h3>
                <p>Présence dans toutes les régions de Tunisie avec un réseau étendu.</p>
            </div>
            <div class="metric-item">
                <h3>Expérience client soignée</h3>
                <p>Service client réactif et interface intuitive pour une expérience optimale.</p>
            </div>
        </div>
    </div>
</section>

<!-- Features Section -->
<section class="features-section">
    <div class="features-container">
        <div class="features-grid">
            <div class="card-glass">
                <h3>Plateforme pour les pros</h3>
                <p>Outils avancés de gestion, tableaux de bord en temps réel et analytics pour optimiser vos opérations logistiques.</p>
            </div>
            <div class="card-glass">
                <h3>Pilotage des livraisons</h3>
                <p>Suivi GPS précis, optimisation automatique des tournées et notification instantanée aux clients.</p>
            </div>
            <div class="card-glass">
                <h3>Fiabilité & transparence</h3>
                <p>Tracking en temps réel, preuves de livraison et reporting détaillé pour une confiance totale.</p>
            </div>
        </div>
    </div>
</section>

<!-- Footer -->
<footer class="footer">
    <div class="footer-container">
        <p>© {{ "now"|date("Y") }} TGV Delivery. Tous droits réservés.</p>
        <p>Projet PIDEV - Gestion des partenaires & zones de livraison</p>
    </div>
</footer>
{% endblock %}

{% block javascripts %}
<script>
// Smooth scroll for navigation
document.querySelectorAll('a[href^="#"]').forEach(anchor => {
    anchor.addEventListener('click', function (e) {
        e.preventDefault();
        const target = document.querySelector(this.getAttribute('href'));
        if (target) {
            target.scrollIntoView({ behavior: 'smooth' });
        }
    });
});

// Add scroll effect to navbar
window.addEventListener('scroll', () => {
    const navbar = document.querySelector('.navbar-delivery');
    if (window.scrollY > 50) {
        navbar.style.background = 'rgba(15, 23, 42, 0.95)';
    } else {
        navbar.style.background = 'rgba(15, 23, 42, 0.8)';
    }
});
</script>
{% endblock %}
