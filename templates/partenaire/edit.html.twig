{% extends 'layout/base_back.html.twig' %}

{% block title %}Modifier partenaire{% endblock %}

{% block page_title %}Modifier un partenaire{% endblock %}

{% block body %}
    <div class="card shadow-sm border-0">
        <div class="card-body">
            {{ include('partenaire/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
        </div>
    </div>

    <div class="d-flex gap-2 mt-3">
        <a class="btn btn-outline-secondary" href="{{ path('app_partenaire_index') }}">Retour à la liste</a>
        {{ include('partenaire/_delete_form.html.twig') }}
    </div>
{% endblock %}
